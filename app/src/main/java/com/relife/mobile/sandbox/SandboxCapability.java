package com.relife.mobile.sandbox;

/** Capabilities a user may independently grant to the phone-side agent sandbox. */
public enum SandboxCapability {
    READ_FILES,
    WRITE_FILES,
    DEVICE_INFO
}
